{
  "name": "strata-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hardened caching proxy Worker for Strata Studio terrain and Overpass data",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check:config": "bun test test/config.test.ts",
    "check:bundle": "bun run check:config && wrangler deploy --dry-run --outdir .wrangler/dry-run --metafile && bun run scripts/assert-bundle.ts .wrangler/dry-run",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.110.0"
  }
}
